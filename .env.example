DATABASE_URL="postgresql://postgres:postgres@localhost:5432/zoorvan?schema=public"
AUTH_SECRET="replace-with-output-of-openssl-rand-base64-32"
AUTH_URL="http://localhost:3000"
ADMIN_EMAIL=""
ADMIN_PASSWORD=""
# Set true only after final product details, policies, and shipping are approved.
COMMERCE_ENABLED="false"
# Optional password recovery (use URL-encoded SMTP credentials)
SMTP_URL=""
EMAIL_FROM=""
# Force a read-only demo even with a database configured
STORE_PREVIEW="false"
